/**
 * The Candy deployment bundle, node half. The layer is its
 * `cordis.patch.yml`; this module exists so the package is a Loader-visible
 * entry with a type face, and registers nothing of its own.
 */

export {}
